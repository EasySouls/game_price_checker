name: games_repository
description: A repository that handles the games and the related actions
version: 0.1.0+1
publish_to: none

environment:
  sdk: '^3.3.0'
  flutter: '^3.19.0'

dependencies:
  firestore_favorites_api:
    path: ../firestore_favorites_api
  flutter:
    sdk: flutter
  games_api:
    path: ../games_api
  meta: ^1.11.0

dev_dependencies:
  very_good_analysis: ^5.1.0
